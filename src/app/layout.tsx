import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { ThemeProvider } from "@/components/theme-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "DVAI — Damn Vulnerable AI Ecosystem",
  description: "Open-source, zero-infrastructure-cost AI red team training range. Player-supplied API keys. Six operations covering logprob side-channels, RAG poisoning, adversarial suffixes, pipeline exploitation, semantic manipulation, and mutation engine bypass.",
  keywords: ["DVAI", "AI red team", "AI security", "adversarial ML", "logprob", "prompt injection", "CTF"],
  authors: [{ name: "DVAI Project" }],
  icons: {
    icon: "data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🛡️</text></svg>",
  },
  openGraph: {
    title: "DVAI — Damn Vulnerable AI Ecosystem",
    description: "AI Red Team Training Range",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ThemeProvider attribute="class" defaultTheme="dark" enableSystem={false}>
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
