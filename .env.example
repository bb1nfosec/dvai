# DVAI — Damn Vulnerable AI Ecosystem

# Database (SQLite for local dev, ignored on Vercel)
DATABASE_URL="file:./db/custom.db"

# Groq API — Player supplies their own key at runtime
# No server-side API key needed

# Vercel KV (for production deployment)
# KVC_REST_API_URL=
# KVC_REST_API_TOKEN=
# KV_REST_API_URL=
# KV_REST_API_TOKEN=

# Optional: Claude API for mutation engine (advanced)
# ANTHROPIC_API_KEY=
